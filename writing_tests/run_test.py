import unittest
from tests.coffee_shop_test import TestCoffeShop

if __name__ == "__main__": 
    unittest.main()

# When we do python3 any file, this code is the file that were running. 
# Python uses __thing__ to keep track for itself
# Python usually gives __main__ as name to the files hes being told to run, 
# so this means hes simply going to run this only if hes being told to